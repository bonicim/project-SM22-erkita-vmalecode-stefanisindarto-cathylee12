name: Example CI

on:
  push:
    branches: 
      - main


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests 
        run: |
          ls
          echo "Running tests...."
